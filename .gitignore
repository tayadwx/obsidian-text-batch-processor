# 依赖（构建时才需要，不进仓库）
node_modules/

# 个人项目记忆（非插件内容，不公开发布）
.workbuddy/

# 系统/编辑器临时文件
.DS_Store
Thumbs.db
*.log

# 本地 GitHub token（含 repo 权限，禁止提交）
.github_token

# 本地部署测试脚本（仅本地用，复制 main.js 到测试 vault，不公开）
scripts/deploy.mjs

# 临时测试 harness（本地验证用，不进仓库）
_harness/
